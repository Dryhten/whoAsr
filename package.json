{
  "private": true,
  "name": "whoasr",
  "scripts": {
    "install:cpu": "uv sync --extra cpu && npm install --prefix frontend",
    "install:gpu": "uv sync --extra gpu && npm install --prefix frontend",
    "dev": "concurrently -n frontend,api -c blue,magenta \"npm run dev:frontend\" \"npm run dev:api\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:api": "uv run python -m api.main",
    "build": "npm run build --prefix frontend",
    "start": "npm run build && uv run python -m api.main"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
