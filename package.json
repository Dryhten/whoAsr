{
  "private": true,
  "name": "whoasr",
  "scripts": {
    "install:cpu": "uv sync --extra cpu && npm install --prefix frontend --legacy-peer-deps",
    "install:gpu": "uv sync --extra gpu && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently -n frontend,api -c blue,magenta \"npm run dev:frontend\" \"npm run dev:api\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:api": "uv run python -m api.main",
    "build": "cd frontend && npm run build",
    "start": "uv run python -m api.main"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
